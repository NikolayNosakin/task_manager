
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Объект.Ссылка.Пустая() Тогда
		Объект.Ответственный = Пользователи.ТекущийПользователь();
	КонецЕсли;
	Элементы.Ответственный.ТолькоПросмотр = НЕ РольДоступна("ПолныеПрава");
КонецПроцедуры

&НаКлиенте
Процедура СписокЗадачЗатраченоЧасовПриИзменении(Элемент)
	Объект.ВсегоЧасов = Объект.СписокЗадач.Итог("ЗатраченоЧасов");
	Элементы.СписокЗадачЗатраченоЧасов.ТекстПодвала = Объект.ВсегоЧасов;
КонецПроцедуры

